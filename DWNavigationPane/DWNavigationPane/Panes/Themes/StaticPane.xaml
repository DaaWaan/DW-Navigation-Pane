<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DWNavigationPane">
    <Style TargetType="{x:Type local:StaticPane}">
        <Setter Property="Background" Value="#FF1A1D22" />
        <Setter Property="Foreground" Value="White" />

        <Setter Property="local:Element.IconSize" Value="18" />
        <Setter Property="local:Element.IconContainerSize" Value="48" />
        <Setter Property="local:Element.BackgroundHover" Value="#FF20242B" />
        <Setter Property="local:Element.ForegroundHover" Value="#FFA6E22E" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:StaticPane}">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>